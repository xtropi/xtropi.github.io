"use client"
import { Divider, Space, Typography } from 'antd';
const { Title } = Typography;
export default function Component() {
  return (
    <main>
      <Title level={4}>Personal-blog styled analitycs for stakers</Title>
      <Divider />
      Under development
    </main>
  );
}
