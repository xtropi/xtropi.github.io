/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
    },
    output: 'export',
    // basePath: "/xtropi.github.io"
};

export default nextConfig;
